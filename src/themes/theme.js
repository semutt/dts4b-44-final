import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#1560bd',
        },
        secondary: {
          main: '#86cef8',
        },
      },
});

export default theme;
